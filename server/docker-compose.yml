version: '3.8'

services:
  web-server:
    build: .
    container_name: python_server
    ports:
      - "443:443"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      # Optional
      - ./config.json:/app/config.json
    restart: unless-stopped
